<%= form_for @post, html: { multipart: true } do |f| %>
  <%= f.file_field :image, class: 'form-control mb-3', required: true %>
  <%= f.hidden_field :image_cache %>
  <%= f.submit 'Upload Post', class: 'btn btn-primary' %>
<% end %>
